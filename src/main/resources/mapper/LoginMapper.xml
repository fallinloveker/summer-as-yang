<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.say.tikujun.mapper.LoginMapper">
    <select id = "getLogin" resultType="int">
        select IFNULL((select 1 as isExist from student where contact = ${contact} and student.password = ${password} limit 1), 0)
    </select>
    <select id = "A_getLogin" resultType="int">
        select IFNULL((select 1 as isExist from administrator where contact = ${contact} and administrator.password = ${password} limit 1), 0)
    </select>
</mapper>